adis - ARMv4T Disassembler
*************************

This is a simple ARMv4T disassembler written in C. ARMv4T binaries are 
piped in to the executable through stdin, are then translated to
ARM assembly through stdout. 

To compile run:
    make

Once compiled, the program can be used as follows:
    ./adis < arm_binary_input > disassembled_output
